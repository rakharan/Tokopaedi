<html>
    <head>
        <title>{{title}}</title>
       <style>
            body { font-family: 'Arial', sans-serif; margin: 0; padding: 0; background-color: #f4f4f4; } .container { max-width: 600px; margin: 0 auto; padding: 20px; background-color: #fff; } img { display: block; margin: auto; } p { margin-bottom: 15px; line-height: 1.6; } h1 { color: #333; } ul { list-style: none; padding: 0; } li { margin-bottom: 5px; }
        </style>
    </head>
    <body>
        {{> header}}
        {{> body}}
        {{> footer}}
    </body>
</html>
